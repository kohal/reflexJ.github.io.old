###java线程池的一些使用

```java
Future future = queryBalanceThreadPool.submit(new Callable() {
                        @Override
                        public Object call() throws Exception {
                            doSomething();
                            return Object;
                        }
                    });
                    resultHolder.add(future);
                }


                for (int i = 0; i < resultHolder.size(); i++) {
                    Future<Object> future = resultHolder.get(i);
                    if (future.isDone()) {
                         Object object= future.get();
                        resultHolder.remove(i);
                        i--;
                    }
                    if (i == resultHolder.size() - 1) i = -1;

                }

```

```java
ExecutorCompletionService executorCompletionService = new ExecutorCompletionService(queryBalanceThreadPool);

```
